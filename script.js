const translations = {
    tr: {
        nav_home: "Ana Sayfa",
        nav_collection: "Koleksiyon",
        nav_verify: "Sorgula",
        nav_contact: "İletişim",
        hero_title: "Zamanın Ötesinde <br> <span class=\"gold-text\">Bir Miras</span>",
        hero_subtitle: "El işçiliğinin zirvesi, asaletin simgesi. Her tanede ayrı bir hikaye.",
        hero_cta: "Koleksiyonu Keşfet",
        intro_heading: "Gelenek ve <span class=\"gold-text\">Lüksün</span> Buluşması",
        intro_text: "Yüzyıllık geleneği modern estetikle harmanlıyoruz. Her bir tesbihimiz, ustalarımızın sabır ve titizlikle işlediği, nesilden nesile aktarılacak birer sanat eseridir.",
        section_collection: "Özel Koleksiyonlar",
        all_collection: "Tüm Koleksiyonu Gör",
        section_masterpiece: "Ustalık Eseri",
        masterpiece_text: "Her bir imame, her bir durak, milimetrik hassasiyetle işlenir. Sadece bir aksesuar değil, ruhunuzu yansıtan bir parça.",
        feat_1: "1. Sınıf Malzeme",
        feat_2: "%100 El İşçiliği",
        feat_3: "Orijinallik Sertifikası",
        footer_brand_desc: "Lüksün ve zarafetin adresi.",
        copyright: "© 2026 Ahmet Yılmaz Rosary. Tüm hakları saklıdır.",
        login_text: "Giriş Yap",
        empty_msg: "Henüz ürün eklenmemiş.",
        collection_empty: "Koleksiyon şu an boş.",
        in_stock: "Stokta Var",
        out_of_stock: "Tükendi",
        examine: "İncele",
        collection_title: "Tüm <span class=\"gold-text\">Koleksiyon</span>",
        filter_search: "Ara",
        search_placeholder: "Ürün ara...",
        filter_categories: "Kategoriler",
        cat_all: "Hepsi",
        cat_amber: "Kehribar",
        cat_oltu: "Oltu Taşı",
        cat_kuka: "Kuka",
        cat_natural: "Doğal Taş",
        listing_info: "Ürün Listeleniyor",
        sort_by: "Sırala:",
        sort_default: "Önerilen",
        sort_price_asc: "Fiyat (Düşükten Yükseğe)",
        sort_price_desc: "Fiyat (Yüksekten Düşüğe)",
        sort_name_asc: "İsim (A-Z)",
        load_more: "Daha Fazla Göster",
        favorites_title: "Favorilerim",
        fav_empty: "Henüz favori ürününüz yok.",
        artist: "Sanatkar :",
        material: "Materyal :",
        bead_count: "Tane Sayısı :",
        bead_size: "Tane Boyutu :",
        motif: "İmame Motifi :",
        imame_size: "İmame Boyutu :",
        wa_info: "Bu ürün hakkında detaylı bilgi alabilirsiniz.",
        wa_btn: "WHATSAPP İLE BİLGİ AL",
        wa_sub: "Ürün hakkında detaylı bilgi için WhatsApp üzerinden iletişime geçin.",
        sku: "Stok kodu:",
        categories: "Kategoriler:",
        verify_title: "Sertifika <span class=\"gold-text\">Doğrulama</span>",
        verify_subtitle: "Ürününüzün orijinalliğini ve sanatkar bilgilerini buradan teyit edebilirsiniz.",
        verify_placeholder: "Seri Numarasını Giriniz...",
        verify_success_title: "Ürün Doğrulandı",
        verify_success_desc: "Bu ürün Ahmet Yılmaz Rosary tarafından onaylı bir orijinal eserdir.",
        verify_date_label: "Doğrulama Tarihi:",
        verify_btn: "SORGULA",
        verify_loading: "Sorgulanıyor...",
        contact_title: "Bizimle <span class=\"gold-text\">İletişime</span> Geçin",
        contact_subtitle: "Sorularınız ve özel tasarım talepleriniz için bize ulaşabilirsiniz.",
        price_ask: "Fiyat Sorunuz",
        contact_header: "Bize <span class=\"gold-text\">Ulaşın</span>",
        contact_phone: "Telefon",
        contact_phone_hours: "Hafta içi her gün 09:00 - 19:00",
        contact_email: "E-Posta",
        contact_email_text: "Tüm sorularınız için bize yazın",
        contact_wa: "WhatsApp",
        contact_wa_text: "Hızlı bilgi ve sipariş hattı",
        contact_wa_btn: "Sohbeti Başlat",
        appointment_title: "Özel <span class=\"gold-text\">Randevu</span> Alın",
        appointment_subtitle: "Koleksiyonumuzu yakından incelemek ve özel tasarımlarımız hakkında bilgi almak için randevu oluşturabilirsiniz.",
        form_name: "Adınız Soyadınız",
        form_date: "Randevu Tarihi",
        form_time: "Randevu Saati",
        form_topic: "Görüşme Konusu (Opsiyonel)",
        form_topic_placeholder: "Hangi koleksiyon veya ürünle ilgileniyorsunuz?",
        form_submit: "RANDEVU TALEBİ GÖNDER",
        login_title: "Giriş Yap",
        login_user: "Kullanıcı Adı veya E-posta",
        login_pass: "Şifre",
        login_btn: "GİRİŞ YAP",
        no_account: "Hesabınız yok mu?",
        register: "Kayıt Ol",
        back_to_web: "Web Sitesine Dön",
        nav_profile: "Profilim",
        logout_text: "Çıkış Yap",
        drawer_subtitle: "Tüm koleksiyonu keşfedin"
    },
    en: {
        nav_home: "Home",
        nav_collection: "Collection",
        nav_verify: "Verify",
        nav_contact: "Contact",
        hero_title: "A Legacy <br> <span class=\"gold-text\">Beyond Time</span>",
        hero_subtitle: "The pinnacle of craftsmanship, the symbol of nobility. A different story in every bead.",
        hero_cta: "Explore Collection",
        intro_heading: "Meeting of <span class=\"gold-text\">Tradition and Luxury</span>",
        intro_text: "We blend century-old tradition with modern aesthetics. Each of our rosaries is a work of art crafted with patience and precision by our masters, to be passed down through generations.",
        section_collection: "Special Collections",
        all_collection: "See Full Collection",
        section_masterpiece: "Masterpiece",
        masterpiece_text: "Every imame, every bead is processed with millimetric precision. Not just an accessory, but a piece reflecting your soul.",
        feat_1: "1st Class Material",
        feat_2: "100% Handcrafted",
        feat_3: "Certificate of Authenticity",
        footer_brand_desc: "The address of luxury and elegance.",
        copyright: "© 2026 Ahmet Yılmaz Rosary. All rights reserved.",
        login_text: "Login",
        empty_msg: "No products added yet.",
        collection_empty: "Collection is currently empty.",
        in_stock: "In Stock",
        out_of_stock: "Out of Stock",
        examine: "View Details",
        collection_title: "Full <span class=\"gold-text\">Collection</span>",
        filter_search: "Search",
        search_placeholder: "Search products...",
        filter_categories: "Categories",
        cat_all: "All",
        cat_amber: "Amber",
        cat_oltu: "Oltu Stone",
        cat_kuka: "Kuka",
        cat_natural: "Natural Stone",
        listing_info: "Products Listed",
        sort_by: "Sort by:",
        sort_default: "Recommended",
        sort_price_asc: "Price (Low to High)",
        sort_price_desc: "Price (High to Low)",
        sort_name_asc: "Name (A-Z)",
        load_more: "Show More",
        favorites_title: "My Favorites",
        fav_empty: "No favorite products yet.",
        artist: "Artist:",
        material: "Material:",
        bead_count: "Bead Count:",
        bead_size: "Bead Size:",
        motif: "Imame Motif:",
        imame_size: "Imame Size:",
        wa_info: "You can get detailed information about this product.",
        wa_btn: "GET INFO VIA WHATSAPP",
        wa_sub: "Contact us on WhatsApp for detailed information about the product.",
        sku: "SKU:",
        categories: "Categories:",
        verify_title: "Certificate <span class=\"gold-text\">Verification</span>",
        verify_subtitle: "Confirm the authenticity and artist information of your product here.",
        verify_placeholder: "Serial Number...",
        verify_success_title: "Product Verified",
        verify_success_desc: "This product is an original masterpiece certified by Ahmet Yılmaz Rosary.",
        verify_date_label: "Verification Date:",
        verify_btn: "VERIFY",
        verify_loading: "Verifying...",
        contact_title: "Contact <span class=\"gold-text\">Us</span>",
        contact_subtitle: "You can reach us for your questions and custom design requests.",
        price_ask: "Contact for Price",
        contact_header: "Get in <span class=\"gold-text\">Touch</span>",
        contact_phone: "Phone",
        contact_phone_hours: "Weekdays 09:00 - 19:00",
        contact_email: "E-Mail",
        contact_email_text: "Write to us for all your questions",
        contact_wa: "WhatsApp",
        contact_wa_text: "Fast information and order line",
        contact_wa_btn: "Start Chat",
        appointment_title: "Get a Special <span class=\"gold-text\">Appointment</span>",
        appointment_subtitle: "You can create an appointment to examine our collection closely and get information about our special designs.",
        form_name: "Full Name",
        form_date: "Appointment Date",
        form_time: "Appointment Time",
        form_topic: "Meeting Topic (Optional)",
        form_topic_placeholder: "Which collection or product are you interested in?",
        form_submit: "SEND APPOINTMENT REQUEST",
        login_title: "Login",
        login_user: "Username or Email",
        login_pass: "Password",
        login_btn: "LOGIN",
        no_account: "Don't have an account?",
        register: "Register",
        back_to_web: "Back to Website",
        nav_profile: "My Profile",
        logout_text: "Logout",
        drawer_subtitle: "Explore our entire collection"
    },
    ar: {
        nav_home: "الصفحة الرئيسية",
        nav_collection: "المجموعة",
        nav_verify: "التحقق",
        nav_contact: "اتصل بنا",
        hero_title: "تراث <br> <span class=\"gold-text\">يتجاوز الزمن</span>",
        hero_subtitle: "قمة الحرفية، رمز النبالة. قصة مختلفة في كل خرزة.",
        hero_cta: "استكشف المجموعة",
        intro_heading: "التقاء <span class=\"gold-text\">التقاليد والرفاهية</span>",
        intro_text: "نمزج بين التقاليد الممتدة لقرون والجمالية الحديثة. كل سبحة من سبحنا هي قطعة فنية صنعها أساتذتنا بصبر ودقة، لتنتقل عبر الأجيال.",
        section_collection: "مجموعات خاصة",
        all_collection: "مشاهدة المجموعة الكاملة",
        section_masterpiece: "تحفة فنية",
        masterpiece_text: "يتم معالجة كل إمامة وكل خرزة بدقة مليمترية. ليست مجرد إكسسوار، بل قطعة تعكس روحك.",
        feat_1: "مواد من الدرجة الأولى",
        feat_2: "صناعة يدوية 100%",
        feat_3: "شهادة الأصالة",
        footer_brand_desc: "عنوان الفخامة والأناقة.",
        copyright: "© 2026 أحمد يلماز للسبح. جميع الحقوق محفوظة.",
        login_text: "تسجيل الدخول",
        empty_msg: "لم يتم إضافة منتجات بعد.",
        collection_empty: "المجموعة فارغة حالياً.",
        in_stock: "متوفر",
        out_of_stock: "نفذت الكمية",
        examine: "فحص",
        collection_title: "المجموعة <span class=\"gold-text\">الكاملة</span>",
        filter_search: "بحث",
        search_placeholder: "البحث عن المنتجات...",
        filter_categories: "الفئات",
        cat_all: "الكل",
        cat_amber: "كهرمان",
        cat_oltu: "حجر أولتو",
        cat_kuka: "كوكا",
        cat_natural: "حجر طبيعي",
        listing_info: "المنتجات المدرجة",
        sort_by: "رتب حسب:",
        sort_default: "موصى به",
        sort_price_asc: "السعر (من الأقل إلى الأعلى)",
        sort_price_desc: "السعر (من الأعلى إلى الأقل)",
        sort_name_asc: "الاسم (أ-ي)",
        load_more: "عرض المزيد",
        favorites_title: "مفضلاتي",
        fav_empty: "لا يوجد منتجات مفضلة بعد.",
        artist: "الفنان:",
        material: "المادة:",
        bead_count: "عدد الخرز:",
        bead_size: "حجم الخرزة:",
        motif: "زخرفة الإمامة:",
        imame_size: "حجم الإمامة:",
        wa_info: "يمكنك الحصول على معلومات مفصلة حول هذا المنتج.",
        wa_btn: "احصل على المعلومات عبر واتساب",
        wa_sub: "اتصل بنا على واتساب للحصول على معلومات مفصلة حول المنتج.",
        sku: "رمز المنتج:",
        categories: "الفئات:",
        verify_title: "التحقق من <span class=\"gold-text\">الشهادة</span>",
        verify_subtitle: "تأكد من أصالة المنتج ومعلومات الفنان هنا.",
        verify_placeholder: "أدخل الرقم التسلسلي...",
        verify_success_title: "تم التحقق من المنتج",
        verify_success_desc: "هذا المنتج تحفة أصلية معتمدة من أحمد يلماز روزاري.",
        verify_date_label: "تاريخ التحقق:",
        verify_btn: "تحقق",
        verify_loading: "جاري التحقق...",
        contact_title: "اتصل <span class=\"gold-text\">بنا</span>",
        contact_subtitle: "يمكنك الوصول إلينا لأسئلتك وطلبات التصميم المخصصة.",
        price_ask: "اتصل للحصول على السعر",
        contact_header: "اتصل <span class=\"gold-text\">بنا</span>",
        contact_phone: "هاتف",
        contact_phone_hours: "أيام الأسبوع 09:00 - 19:00",
        contact_email: "البريد الإلكتروني",
        contact_email_text: "اكتب إلينا لجميع أسئلتك",
        contact_wa: "واتساب",
        contact_wa_text: "خط المعلومات السريع والطلبات",
        contact_wa_btn: "ابدأ الدردشة",
        appointment_title: "احصل على <span class=\"gold-text\">موعد</span> خاص",
        appointment_subtitle: "يمكنك إنشاء موعد لفحص مجموعتنا عن قرب والحصول على معلومات حول تصاميمنا الخاصة.",
        form_name: "الاسم الكامل",
        form_date: "تاريخ الموعد",
        form_time: "وقت الموعد",
        form_topic: "موضوع الاجتماع (اختياري)",
        form_topic_placeholder: "ما هي المجموعة أو المنتج الذي تهتم به؟",
        form_submit: "إرسال طلب موعد",
        login_title: "تسجيل الدخول",
        login_user: "اسم المستخدم أو البريد الإلكتروني",
        login_pass: "كلمة المرور",
        login_btn: "تسجيل الدخول",
        no_account: "ليس لديك حساب؟",
        register: "سجل الآن",
        back_to_web: "العودة إلى الموقع",
        nav_profile: "ملفي الشخصي",
        logout_text: "تسجيل الخروج",
        drawer_subtitle: "استكشف مجموعتنا كاملة"
    },
    ru: {
        nav_home: "Главная",
        nav_collection: "Коллекция",
        nav_verify: "Проверить",
        nav_contact: "Контакт",
        hero_title: "Наследие <br> <span class=\"gold-text\">Вне Времени</span>",
        hero_subtitle: "Вершина мастерства, символ благородства. Своя история в каждой бусине.",
        hero_cta: "Изучить Коллекцию",
        intro_heading: "Встреча <span class=\"gold-text\">Традиций и Роскоши</span>",
        intro_text: "Мы сочетаем многовековые традиции с современной эстетикой. Каждые наши четки — это произведение искусства, созданное нашими мастерами с терпением и точностью, которое будет передаваться из поколения в поколение.",
        section_collection: "Специальные Коллекции",
        all_collection: "Посмотреть Всю Коллекцию",
        section_masterpiece: "Шедевр",
        masterpiece_text: "Каждое имаме, каждая бусина обрабатывается с миллиметровой точностью. Не просто аксессуар, а частичка вашей души.",
        feat_1: "Материалы 1-го Класса",
        feat_2: "100% Ручная Работа",
        feat_3: "Сертификат Подлинности",
        footer_brand_desc: "Адрес роскоши и элегантности.",
        copyright: "© 2026 Ахмет Йылмаз Розарий. Все права защищены.",
        login_text: "Войти",
        empty_msg: "Товары еще не добавлены.",
        collection_empty: "Коллекция в данный момент пуста.",
        in_stock: "В наличии",
        out_of_stock: "Нет в наличии",
        examine: "Изучить",
        collection_title: "Полная <span class=\"gold-text\">Коллекция</span>",
        filter_search: "Поиск",
        search_placeholder: "Поиск товаров...",
        filter_categories: "Категории",
        cat_all: "Все",
        cat_amber: "Янтарь",
        cat_oltu: "Ольту",
        cat_kuka: "Кука",
        cat_natural: "Натуральный камень",
        listing_info: "Товаров перечислено",
        sort_by: "Сортировать по:",
        sort_default: "Рекомендуемые",
        sort_price_asc: "Цена (по возрастанию)",
        sort_price_desc: "Цена (по убыванию)",
        sort_name_asc: "Имя (А-Я)",
        load_more: "Показать больше",
        favorites_title: "Мое Избранное",
        fav_empty: "В избранном пока ничего нет.",
        serial_no: "Серийный номер:",
        artist: "Мастер:",
        material: "Материал:",
        bead_count: "Количество бусин:",
        bead_size: "Размер бусины:",
        motif: "Мотив имаме:",
        imame_size: "Размер имаме:",
        wa_info: "Вы можете получить подробную информацию об этом товаре.",
        wa_btn: "ПОЛУЧИТЬ ИНФО В WHATSAPP",
        wa_sub: "Свяжитесь с нами в WhatsApp для получения подробной информации о товаре.",
        sku: "Артикул:",
        categories: "Категории:",
        verify_title: "Проверка <span class=\"gold-text\">Сертификата</span>",
        verify_subtitle: "Подтвердите подлинность и информацию о мастере здесь.",
        verify_placeholder: "Серийный номер...",
        verify_code_placeholder: "Пароль сертификата...",
        serial_no_label: "Серийный номер",
        cert_code_label: "Пароль сертификата",
        verify_success_title: "Продукт проверен",
        verify_success_desc: "Этот продукт является оригинальным шедевром, сертифицированным Ahmet Yılmaz Rosary.",
        verify_date_label: "Дата проверки:",
        verify_btn: "ПРОВЕРИТЬ",
        verify_loading: "Проверка...",
        contact_title: "Свяжитесь <span class=\"gold-text\">с нами</span>",
        contact_subtitle: "Вы можете связаться с нами по вопросам и запросам на индивидуальный дизайн.",
        price_ask: "Уточнить цену",
        contact_header: "Свяжитесь <span class=\"gold-text\">с нами</span>",
        contact_phone: "Телефон",
        contact_phone_hours: "Будни 09:00 - 19:00",
        contact_email: "E-Mail",
        contact_email_text: "Пишите нам по всем вопросам",
        contact_wa: "WhatsApp",
        contact_wa_text: "Линия быстрой информации и заказов",
        contact_wa_btn: "Начать чат",
        appointment_title: "Специальная <span class=\"gold-text\">Запись</span>",
        appointment_subtitle: "Вы можете записаться на прием, чтобы поближе познакомиться с нашей коллекцией и получить информацию о наших специальных дизайнах.",
        form_name: "Имя Фамилия",
        form_date: "Дата встречи",
        form_time: "Время встречи",
        form_topic: "Тема встречи (необязательно)",
        form_topic_placeholder: "Какая коллекция или товар вас интересует?",
        form_submit: "ОТПРАВИТЬ ЗАПРОС",
        login_title: "Вход",
        login_user: "Имя пользователя или Email",
        login_pass: "Пароль",
        login_btn: "ВОЙТИ",
        no_account: "Нет аккаунта?",
        register: "Регистрация",
        back_to_web: "Назад на сайт",
        nav_profile: "Мой Профиль",
        logout_text: "Выйти",
        drawer_subtitle: "Исследуйте нашу коллекцию"
    },
    de: {
        nav_home: "Startseite",
        nav_collection: "Kollektion",
        nav_verify: "Prüfen",
        nav_contact: "Kontakt",
        hero_title: "Ein Erbe <br> <span class=\"gold-text\">Jenseits der Zeit</span>",
        hero_subtitle: "Der Gipfel der Handwerkskunst, das Symbol des Adels. In jeder Perle eine andere Geschichte.",
        hero_cta: "Kollektion Entdecken",
        intro_heading: "Begegnung von <span class=\"gold-text\">Tradition und Luxus</span>",
        intro_text: "Wir verbinden jahrhundertealte Tradition mit moderner Ästhetik. Jedes unserer Gebetskette ist ein Kunstwerk, das von unseren Meistern mit Geduld und Präzision gefertigt wurde, um über Generationen hinweg weitergegeben zu werden.",
        section_collection: "Spezialkollektionen",
        all_collection: "Ganze Kollektion Sehen",
        section_masterpiece: "Meisterwerk",
        masterpiece_text: "Jeder Imame, jede Perle wird mit millimetergenauer Präzision verarbeitet. Nicht nur ein Accessoire, sondern ein Teil, das Ihre Seele widerspiegelt.",
        feat_1: "Erstklassiges Material",
        feat_2: "100% Handarbeit",
        feat_3: "Echtheitszertifikat",
        footer_brand_desc: "Die Adresse für Luxus und Eleganz.",
        copyright: "© 2026 Ahmet Yılmaz Rosary. Alle Rechte vorbehalten.",
        login_text: "Anmelden",
        empty_msg: "Noch keine Produkte hinzugefügt.",
        collection_empty: "Die Kollektion ist zur Zeit leer.",
        in_stock: "Auf Lager",
        out_of_stock: "Ausverkauft",
        examine: "Prüfen",
        collection_title: "Vollständige <span class=\"gold-text\">Kollektion</span>",
        filter_search: "Suche",
        search_placeholder: "Produkte suchen...",
        filter_categories: "Kategorien",
        cat_all: "Alle",
        cat_amber: "Bernstein",
        cat_oltu: "Oltu Stein",
        cat_kuka: "Kuka",
        cat_natural: "Naturstein",
        listing_info: "Produkte Gefunden",
        sort_by: "Sortieren nach:",
        sort_default: "Empfohlen",
        sort_price_asc: "Preis (Aufsteigend)",
        sort_price_desc: "Preis (Absteigend)",
        sort_name_asc: "Name (A-Z)",
        load_more: "Mehr Anzeigen",
        favorites_title: "Meine Favoriten",
        fav_empty: "Noch keine Favoriten.",
        serial_no: "Seriennummer:",
        artist: "Künstler:",
        material: "Material:",
        bead_count: "Perlenanzahl:",
        bead_size: "Perlengröße:",
        motif: "Imame Motiv:",
        imame_size: "Imame Größe:",
        wa_info: "Sie können detaillierte Informationen zu diesem Produkt erhalten.",
        wa_btn: "INFO PER WHATSAPP ERHALTEN",
        wa_sub: "Kontaktieren Sie uns per WhatsApp für detaillierte Informationen zum Produkt.",
        sku: "Artikelnummer:",
        categories: "Kategorien:",
        verify_placeholder: "Seriennummer...",
        verify_code_placeholder: "Zertifikatspasswort...",
        serial_no_label: "Seriennummer",
        cert_code_label: "Zertifikatspasswort",
        verify_success_title: "Produkt verifiziert",
        verify_success_desc: "Dieses Produkt ist ein originales Meisterwerk, zertifiziert von Ahmet Yılmaz Rosary.",
        verify_date_label: "Verifizierungsdatum:",
        verify_btn: "PRÜFEN",
        verify_loading: "Wird geprüft...",
        contact_title: "Kontaktieren Sie <span class=\"gold-text\">uns</span>",
        contact_subtitle: "Sie können uns für Ihre Fragen und individuellen Designwünsche erreichen.",
        price_ask: "Preis anfragen",
        contact_header: "Kontaktieren Sie <span class=\"gold-text\">uns</span>",
        contact_phone: "Telefon",
        contact_phone_hours: "Wochentage 09:00 - 19:00",
        contact_email: "E-Mail",
        contact_email_text: "Schreiben Sie uns bei Fragen",
        contact_wa: "WhatsApp",
        contact_wa_text: "Schnelle Info- und Bestellhotline",
        contact_wa_btn: "Chat Starten",
        appointment_title: "Besonderen <span class=\"gold-text\">Termin</span> Vereinbaren",
        appointment_subtitle: "Vereinbaren Sie einen Termin, um unsere Kollektion aus der Nähe zu betrachten und Informationen zu unseren Designs zu erhalten.",
        form_name: "Vorname Nachname",
        form_date: "Termindatum",
        form_time: "Terminuhrzeit",
        form_topic: "Besprechungsthema (Optional)",
        form_topic_placeholder: "Für welche Kollektion oder welches Produkt interessieren Sie sich?",
        form_submit: "TERMINANFRAGE SENDEN",
        login_title: "Anmelden",
        login_user: "Benutzername oder E-Mail",
        login_pass: "Passwort",
        login_btn: "ANMELDEN",
        no_account: "Noch kein Konto?",
        register: "Registrieren",
        back_to_web: "Zurück zur Website",
        nav_profile: "Mein Profil",
        logout_text: "Abmelden",
        drawer_subtitle: "Entdecken Sie unsere Kollektion"
    }
};

window.setLanguage = (lang) => {
    localStorage.setItem('selectedLang', lang);
    applyLanguage(lang);
};

const applyLanguage = (lang) => {
    const t = translations[lang] || translations.tr;

    // Update all elements with data-i18n attribute
    document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (t[key]) {
            el.innerHTML = t[key];
        }
    });

    // Update placeholders
    document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
        const key = el.getAttribute('data-i18n-placeholder');
        if (t[key]) {
            el.placeholder = t[key];
        }
    });

    // Update current lang text
    const currentLangEl = document.getElementById('currentLang');
    if (currentLangEl) currentLangEl.textContent = lang.toUpperCase();

    // Handle RTL
    if (lang === 'ar') {
        document.documentElement.setAttribute('dir', 'rtl');
        document.documentElement.setAttribute('lang', 'ar');
    } else {
        document.documentElement.setAttribute('dir', 'ltr');
        document.documentElement.setAttribute('lang', lang);
    }

    // Refresh active state in dropdown
    document.querySelectorAll('.lang-option').forEach(opt => {
        opt.classList.remove('active');
        if (opt.textContent.toLowerCase().includes(lang)) {
            opt.classList.add('active');
        }
    });
};

document.addEventListener('DOMContentLoaded', () => {
    const savedLang = localStorage.getItem('selectedLang') || 'tr';
    applyLanguage(savedLang);

    // Global UI Elements
    const navbar = document.querySelector('.navbar');
    const mobileNav = document.getElementById('mobileNav');
    const hamburger = document.querySelector('.hamburger');

    // State
    let products = [];
    let categories = [];
    let showPrice = true; // Default to true
    let favorites = JSON.parse(localStorage.getItem('ahmetFavorites')) || [];
    let currentCategory = 'all';
    let searchQuery = '';

    // --- Dynamic Product Data Handling ---
    const getProducts = async () => {
        try {
            // First get settings
            try {
                const settingRes = await fetch('/api/settings');
                const settings = await settingRes.json();
                if (settings.show_price && settings.show_price === 'false') {
                    showPrice = false;
                }
                // Apply Site Theme
                const activeTheme = settings.site_theme || 'white-black';
                document.body.classList.remove('theme-light', 'theme-gold', 'theme-white-black', 'theme-black-white', 'theme-black-gold');
                document.body.classList.add('theme-' + activeTheme);
            } catch (e) { console.error('Ayar hatası:', e); }

            // Add cache busting for mobile sync
            const res = await fetch('/api/products?t=' + new Date().getTime());
            products = await res.json();
            window.allProducts = products; // Make globally accessible for urun.html
            console.log('✅ Veritabanı bağlantısı başarılı. Ürün sayısı:', products.length);

            updateUIGrids();
            updateAllHeartIcons();
        } catch (err) {
            console.error('❌ Ürünler yüklenirken hata oluştu:', err);
        }
    };

    const fetchCategories = async () => {
        try {
            const res = await fetch('/api/categories');
            categories = await res.json();
            renderCategoryFilters();
        } catch (err) {
            console.error('Kategoriler yüklenemedi:', err);
        }
    };

    const renderCategoryFilters = () => {
        const filterList = document.getElementById('categoryFilters');
        const drawerGrid = document.getElementById('drawerCategoryGrid');
        if (!filterList) return;

        const lang = localStorage.getItem('selectedLang') || 'tr';
        const t = translations[lang] || translations.tr;

        // 1. Render Top Bar Chips
        let barHtml = `<li><a href="#" class="${currentCategory === 'all' ? 'active' : ''}" data-cat="all" data-i18n="cat_all">${t.cat_all || 'Hepsi'}</a></li>`;

        // 2. Render Drawer Grid
        let drawerHtml = `
            <a href="#" class="cat-card ${currentCategory === 'all' ? 'active' : ''}" data-cat="all">
                <i class="fas fa-layer-group"></i>
                <span data-i18n="cat_all">${t.cat_all || 'Hepsi'}</span>
            </a>
        `;

        const catMap = {
            'Kehribar': { key: 'cat_amber', icon: 'fas fa-gem' },
            'Oltu Taşı': { key: 'cat_oltu', icon: 'fas fa-shield-alt' },
            'Kuka': { key: 'cat_kuka', icon: 'fas fa-tree' },
            'Doğal Taş': { key: 'cat_natural', icon: 'fas fa-cube' }
        };

        categories.forEach(cat => {
            const config = catMap[cat.name] || { key: `cat_${cat.name.toLowerCase().replace(/\s+/g, '_')}`, icon: 'fas fa-ellipsis-h' };
            const displayName = t[config.key] || cat.name;

            barHtml += `<li><a href="#" class="${currentCategory === cat.name ? 'active' : ''}" data-cat="${cat.name}" data-i18n="${config.key}">${displayName}</a></li>`;

            drawerHtml += `
                <a href="#" class="cat-card ${currentCategory === cat.name ? 'active' : ''}" data-cat="${cat.name}">
                    <i class="${config.icon}"></i>
                    <span data-i18n="${config.key}">${displayName}</span>
                </a>
            `;
        });

        filterList.innerHTML = barHtml;
        if (drawerGrid) drawerGrid.innerHTML = drawerHtml;

        // Attach event listeners to both
        const allCategoryLinks = document.querySelectorAll('#categoryFilters a, .cat-card');
        allCategoryLinks.forEach(link => {
            link.addEventListener('click', (e) => {
                e.preventDefault();
                currentCategory = link.getAttribute('data-cat');

                // Update active state everywhere
                allCategoryLinks.forEach(a => a.classList.remove('active'));
                document.querySelectorAll(`[data-cat="${currentCategory}"]`).forEach(a => a.classList.add('active'));

                // Close drawer if it's open
                const drawerOverlay = document.getElementById('categoryDrawerOverlay');
                if (drawerOverlay) {
                    drawerOverlay.classList.remove('active');
                    document.body.classList.remove('no-scroll');
                }

                filterProducts();
            });
        });
    };

    const filterProducts = () => {
        let filtered = [...products];

        // Apply category filter
        if (currentCategory !== 'all') {
            filtered = filtered.filter(p => p.category === currentCategory);
        }

        // Apply search filter
        if (searchQuery) {
            filtered = filtered.filter(p =>
                p.name.toLowerCase().includes(searchQuery) ||
                (p.category && p.category.toLowerCase().includes(searchQuery))
            );
        }

        // Apply sorting
        const sortVal = document.getElementById('sortSelect')?.value || 'default';
        if (sortVal === 'price-asc') {
            filtered.sort((a, b) => {
                const pA = parseFloat(a.price.replace(/[^\d.]/g, '')) || 0;
                const pB = parseFloat(b.price.replace(/[^\d.]/g, '')) || 0;
                return pA - pB;
            });
        } else if (sortVal === 'price-desc') {
            filtered.sort((a, b) => {
                const pA = parseFloat(a.price.replace(/[^\d.]/g, '')) || 0;
                const pB = parseFloat(b.price.replace(/[^\d.]/g, '')) || 0;
                return pB - pA;
            });
        } else if (sortVal === 'name-asc') {
            filtered.sort((a, b) => a.name.localeCompare(b.name));
        }

        // Update list
        renderCollectionList(filtered);
    };

    const renderCollectionList = (items) => {
        const collectionGrid = document.getElementById('fullGallery') || document.getElementById('catalogGrid');
        if (!collectionGrid) return;

        const lang = localStorage.getItem('selectedLang') || 'tr';
        const t = translations[lang] || translations.tr;

        collectionGrid.innerHTML = items.length > 0
            ? items.map(p => renderProductCard(p)).join('')
            : `<p class="empty-msg">${t.collection_empty}</p>`;

        const countEl = document.getElementById('productCount');
        if (countEl) countEl.textContent = items.length;

        // Restart animations for new items
        observeAnimations();
    };

    const updateUIGrids = () => {
        const lang = localStorage.getItem('selectedLang') || 'tr';
        const t = translations[lang] || translations.tr;

        // 1. Home Page Grid (ID: gallery or .product-grid)
        const homeGrid = document.getElementById('gallery') || document.querySelector('.product-grid');
        if (homeGrid) {
            let homeProducts = products.filter(p => p.featured === 1 || p.featured === true);
            if (homeProducts.length === 0) homeProducts = products.slice(0, 4);
            else homeProducts = homeProducts.slice(0, 4);

            homeGrid.innerHTML = homeProducts.length > 0
                ? homeProducts.map(p => renderProductCard(p)).join('')
                : `<p class="empty-msg">${t.empty_msg}</p>`;
        }

        // 2. Collection Page Grid (ID: fullGallery or catalogGrid)
        const collectionGrid = document.getElementById('fullGallery') || document.getElementById('catalogGrid');
        if (collectionGrid) {
            collectionGrid.innerHTML = products.length > 0
                ? products.map(p => renderProductCard(p)).join('')
                : `<p class="empty-msg">${t.collection_empty}</p>`;

            const countEl = document.getElementById('productCount');
            if (countEl) countEl.textContent = products.length;
        }
    };

    const renderProductCard = (product) => {
        const lang = localStorage.getItem('selectedLang') || 'tr';
        const t = translations[lang] || translations.tr;
        const primaryImage = (product.images && product.images[0]) || 'assets/amber-tesbih.png';
        const isOutOfStock = product.in_stock === 0 || product.in_stock === false;
        const isFav = favorites.some(f => f.id === product.id);

        const priceHtml = showPrice
            ? `<div class="price">${product.price}</div>`
            : `<div class="price" style="filter: blur(5px); opacity: 0.7; user-select: none;">₺00.000</div>`;

        return `
            <div class="card fade-in-up animate-on-scroll" onclick="openDetails(${product.id})">
                <div class="card-image">
                    <img src="${primaryImage}" alt="${product.name}" style="filter: ${product.filter || ''};">
                    <button class="btn-fav ${isFav ? 'active' : ''}" onclick="event.stopPropagation(); toggleFavorite(${product.id}, this)">
                        <i class="${isFav ? 'fas' : 'far'} fa-heart"></i>
                    </button>
                    ${isOutOfStock ? `<div class="out-of-stock-badge">${t.out_of_stock}</div>` : ''}
                </div>
                <div class="card-info">
                    <h3>${product.name}</h3>
                    <p class="category-name">${product.category}</p>
                    ${priceHtml}
                </div>
            </div>
        `;
    };

    // --- Mobile Lang FAB ---
    const mobileFab = document.getElementById('mobileLangFab');
    if (mobileFab) {
        mobileFab.addEventListener('click', (e) => {
            mobileFab.classList.toggle('open');
            e.stopPropagation();
        });
    }

    document.addEventListener('click', () => {
        if (mobileFab) mobileFab.classList.remove('open');
    });

    // --- Kebab Menu Logic ---
    const kebabBtn = document.getElementById('kebabBtn');
    const kebabMenu = document.getElementById('kebabMenu');

    if (kebabBtn && kebabMenu) {
        kebabBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            kebabMenu.classList.toggle('active');
        });

        document.addEventListener('click', () => {
            kebabMenu.classList.remove('active');
        });
    }

    // --- Filter Toggle Logic (Mobile) ---
    const filterToggle = document.getElementById('filterToggle');
    const filterSidebar = document.querySelector('.collection-sidebar');
    if (filterToggle && filterSidebar) {
        filterToggle.addEventListener('click', () => {
            filterSidebar.classList.toggle('active');
        });
    }

    // --- Sorting Logic ---
    const sortSelect = document.getElementById('sortSelect');
    if (sortSelect) {
        sortSelect.addEventListener('change', () => {
            filterProducts();
        });
    }

    // --- Search Logic ---
    window.openDetails = (productId) => {
        window.location.href = `urun.html?id=${productId}`;
    };

    window.closePopup = () => {
        // Obsolete
    };

    // --- Favorites Logic ---
    window.toggleFavorite = (id, btn) => {
        const product = products.find(p => p.id === id);
        if (!product) return;

        const index = favorites.findIndex(f => f.id === id);
        if (index === -1) {
            favorites.push({
                id: product.id,
                name: product.name,
                price: product.price,
                img: (product.images && product.images[0]) || 'assets/amber-tesbih.png',
                filter: product.filter || ''
            });
            btn.classList.add('active');
            btn.querySelector('i').className = 'fas fa-heart';
        } else {
            favorites.splice(index, 1);
            btn.classList.remove('active');
            btn.querySelector('i').className = 'far fa-heart';
        }
        localStorage.setItem('ahmetFavorites', JSON.stringify(favorites));
        renderFavoritesDrawer();
    };

    const updateAllHeartIcons = () => {
        document.querySelectorAll('.btn-fav').forEach(btn => {
            const card = btn.closest('.product-card');
            if (card) {
                // We'd need the ID here, but for simplicity we rely on the onclick binding
            }
        });
    };

    const renderFavoritesDrawer = () => {
        const favList = document.getElementById('favList');
        if (!favList) return;

        if (favorites.length === 0) {
            favList.innerHTML = '<p class="empty-msg">Henüz favori ürününüz yok.</p>';
            return;
        }

        favList.innerHTML = favorites.map(item => `
            <div class="fav-item">
                <img src="${item.img}" style="filter: ${item.filter}">
                <div class="fav-info">
                    <h4>${item.name}</h4>
                    <span class="price">${item.price}</span>
                    <button onclick="toggleFavorite(${item.id}, this); renderFavoritesDrawer();" class="btn-remove-fav">Kaldır</button>
                </div>
            </div>
        `).join('');
    };

    // --- Global UI Events ---
    if (navbar) {
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) navbar.classList.add('scrolled');
            else navbar.classList.remove('scrolled');
        });
    }

    if (hamburger && mobileNav) {
        hamburger.addEventListener('click', () => {
            hamburger.classList.toggle('active');
            mobileNav.classList.toggle('open');
            document.body.classList.toggle('no-scroll');
        });
    }

    // Removal of modal interaction handlers

    // --- Login Logic ---
    const loginForm = document.getElementById('loginForm');
    if (loginForm) {
        loginForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const usernameInput = document.getElementById('username').value;
            const passwordInput = document.getElementById('password').value;
            const msgDiv = document.getElementById('loginMessage');

            // Simple mock authentication
            // In a real app, you would send a POST request to /api/login
            if ((usernameInput === 'Admin' || usernameInput === 'admin') && passwordInput === 'admin123') {
                msgDiv.style.color = 'var(--gold)';
                msgDiv.textContent = 'Giriş başarılı! Yönlendiriliyorsunuz...';

                localStorage.setItem('isAdmin', 'true');
                localStorage.setItem('username', 'Admin'); // Set consistent username

                setTimeout(() => {
                    window.location.href = 'admin.html';
                }, 1000);
            } else {
                msgDiv.style.color = '#f44336';
                msgDiv.textContent = 'Hatalı kullanıcı adı veya şifre!';

                // Shake effect for error
                const btn = loginForm.querySelector('button');
                btn.classList.add('shake');
                setTimeout(() => btn.classList.remove('shake'), 500);
            }
        });
    }

    // --- Animation Logic ---
    const observeAnimations = () => {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('is-visible');
                    observer.unobserve(entry.target); // Only animate once
                }
            });
        }, {
            threshold: 0.15
        });

        document.querySelectorAll('.animate-on-scroll, .section-title, .hero-content').forEach(el => {
            el.classList.add('animate-on-scroll');
            observer.observe(el);
        });
    };

    // Apply animation classes to existing elements
    const initAnimations = () => {
        const productsGrid = document.querySelector('.product-grid') || document.getElementById('gallery');
        if (productsGrid) {
            productsGrid.classList.add('animate-on-scroll');
        }
        observeAnimations();
    };

    // Call initAnimations on load
    initAnimations();

    // --- Certificate Verification Logic ---
    const verifyBtn = document.getElementById('verifyBtn');
    const serialInput = document.getElementById('serialInput');
    const verifyResult = document.getElementById('verifyResult');

    if (verifyBtn && serialInput && verifyResult) {
        verifyBtn.addEventListener('click', async () => {
            const lang = localStorage.getItem('selectedLang') || 'tr';
            const t = translations[lang] || translations.tr;
            const serial = (serialInput.value || '').trim();

            if (!serial) {
                alert(t.verify_error_msg || (lang === 'tr' ? 'Lütfen seri numarasını giriniz.' : 'Please enter serial number.'));
                return;
            }

            verifyBtn.innerHTML = `<i class="fas fa-spinner fa-spin"></i> ${t.verify_loading}`;
            verifyBtn.disabled = true;
            if (verifyResult) verifyResult.style.display = 'none';

            try {
                const res = await fetch(`/api/verify-certificate?serial=${encodeURIComponent(serial)}`);
                const data = await res.json();

                if (verifyResult) verifyResult.style.display = 'block';

                if (data.success) {
                    const p = data.product;
                    const img = (p.images && p.images[0]) ? p.images[0] : 'assets/amber-tesbih.png';

                    verifyResult.innerHTML = `
                        <div class="result-card success fade-in-up">
                            <div class="result-header">
                                <i class="fas fa-check-circle"></i>
                                <h3>${t.verify_success_title}</h3>
                            </div>
                            <div class="result-body has-img">
                                <div class="result-img-wrapper">
                                    <img src="${img}" alt="${p.name}" class="result-img">
                                </div>
                                <div class="result-info">
                                    <h4>${p.name}</h4>
                                    <div class="result-specs-grid">
                                        <div class="res-spec-item">
                                            <span>${t.material}</span>
                                            <span>${p.material || '-'}</span>
                                        </div>
                                        <div class="res-spec-item">
                                            <span>${t.artist}</span>
                                            <span>${p.artist || 'Ahmet Yılmaz'}</span>
                                        </div>
                                        <div class="res-spec-item">
                                            <span>${t.bead_count}</span>
                                            <span>${p.bead_count || p.beadCount || '-'}</span>
                                        </div>
                                        <div class="res-spec-item">
                                            <span>${t.bead_size}</span>
                                            <span>${p.bead_size || p.beadSize || '-'}</span>
                                        </div>
                                        <div class="res-spec-item">
                                            <span>${t.motif}</span>
                                            <span>${p.motif || '-'}</span>
                                        </div>
                                        <div class="res-spec-item">
                                            <span>${t.imame_size}</span>
                                            <span>${p.imame_size || p.imameSize || '-'}</span>
                                        </div>
                                    </div>
                                    <p class="verify-badge"><i class="fas fa-certificate"></i> Verified Original</p>
                                </div>
                            </div>
                            <div class="verification-footer">
                                <p class="verification-date">${t.verify_date_label} ${new Date().toLocaleDateString(lang === 'ar' ? 'ar-SA' : 'tr-TR')}</p>
                            </div>
                        </div>
                    `;
                } else {
                    verifyResult.innerHTML = `
                        <div class="result-card error fade-in-up">
                            <i class="fas fa-times-circle"></i>
                            <h3>Kayıt Bulunamadı</h3>
                            <p>${data.message}</p>
                        </div>
                    `;
                }
            } catch (err) {
                console.error(err);
                if (verifyResult) {
                    verifyResult.innerHTML = `<p class="error-msg">Error occurred. please try again later.</p>`;
                    verifyResult.style.display = 'block';
                }
            } finally {
                verifyBtn.innerHTML = t.verify_btn;
                verifyBtn.disabled = false;
            }
        });
    }

    // --- Search Modal Logic ---
    const searchModal = document.getElementById('searchModal');
    const searchCloseBtn = document.getElementById('searchCloseBtn');
    const searchInput = document.getElementById('searchInput') || document.getElementById('searchInputModal');
    const searchResults = document.getElementById('searchResults');
    const searchTriggers = document.querySelectorAll('#searchBtn, .search-trigger');

    if (searchTriggers.length > 0 && searchModal) {
        // Open search modal
        searchTriggers.forEach(btn => {
            btn.addEventListener('click', (e) => {
                e.preventDefault();
                searchModal.classList.add('active');
                document.body.classList.add('no-scroll');

                // Close mobile nav if it's open
                if (hamburger && mobileNav && mobileNav.classList.contains('open')) {
                    hamburger.classList.remove('active');
                    mobileNav.classList.remove('open');
                }

                setTimeout(() => {
                    if (searchInput) searchInput.focus();
                }, 100);
            });
        });

        // Close search modal
        const closeSearch = () => {
            searchModal.classList.remove('active');
            document.body.classList.remove('no-scroll');
            if (searchInput) searchInput.value = '';
            if (searchResults) searchResults.innerHTML = '';
        };

        if (searchCloseBtn) {
            searchCloseBtn.addEventListener('click', closeSearch);
        }

        // Close on background click
        searchModal.addEventListener('click', (e) => {
            if (e.target === searchModal) {
                closeSearch();
            }
        });

        // Close on ESC key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && searchModal.classList.contains('active')) {
                closeSearch();
            }
        });

        // Search functionality
        if (searchInput && searchResults) {
            searchInput.addEventListener('input', (e) => {
                const query = e.target.value.trim().toLowerCase();

                if (query.length === 0) {
                    searchResults.innerHTML = '';
                    return;
                }

                // Filter products
                const filteredProducts = products.filter(p => {
                    return p.name.toLowerCase().includes(query) ||
                        (p.category && p.category.toLowerCase().includes(query)) ||
                        (p.material && p.material.toLowerCase().includes(query)) ||
                        (p.artist && p.artist.toLowerCase().includes(query));
                });

                // Display results
                if (filteredProducts.length === 0) {
                    searchResults.innerHTML = '<div class="search-no-results">Ürün bulunamadı</div>';
                } else {
                    searchResults.innerHTML = filteredProducts.map(p => {
                        const img = (p.images && p.images[0]) || 'assets/amber-tesbih.png';
                        return `
                            <div class="search-result-item" onclick="window.location.href='urun.html?id=${p.id}'">
                                <img src="${img}" alt="${p.name}" class="search-result-img" style="filter: ${p.filter || ''};">
                                <div class="search-result-info">
                                    <h4>${p.name}</h4>
                                    <p>${p.category || ''} ${p.material ? '• ' + p.material : ''}</p>
                                </div>
                            </div>
                        `;
                    }).join('');
                }
            });
        }
    }

    // --- Category Drawer Logic ---
    const categoryDrawerOverlay = document.getElementById('categoryDrawerOverlay');
    const openCategoryDrawer = document.getElementById('openCategoryDrawer');
    const closeCategoryDrawer = document.getElementById('closeCategoryDrawer');
    const drawerSearchInput = document.getElementById('drawerSearchInput');

    if (categoryDrawerOverlay && openCategoryDrawer) {
        openCategoryDrawer.addEventListener('click', () => {
            categoryDrawerOverlay.classList.add('active');
            document.body.classList.add('no-scroll');
            if (drawerSearchInput) drawerSearchInput.focus();
        });

        const closeDrawer = () => {
            categoryDrawerOverlay.classList.remove('active');
            document.body.classList.remove('no-scroll');
        };

        if (closeCategoryDrawer) closeCategoryDrawer.addEventListener('click', closeDrawer);

        categoryDrawerOverlay.addEventListener('click', (e) => {
            if (e.target === categoryDrawerOverlay) closeDrawer();
        });

        // Drawer Category Search
        if (drawerSearchInput) {
            drawerSearchInput.addEventListener('input', (e) => {
                const query = e.target.value.toLowerCase();
                document.querySelectorAll('.cat-card').forEach(card => {
                    const name = card.querySelector('span').textContent.toLowerCase();
                    card.style.display = name.includes(query) ? 'flex' : 'none';
                });
            });
        }
    }

    // Initial Load
    getProducts();
    fetchCategories();
    renderFavoritesDrawer();

    // Listen to collection page specific search input
    const collectionSearch = document.getElementById('searchInput');
    if (collectionSearch && !collectionSearch.closest('.search-modal')) {
        collectionSearch.addEventListener('input', (e) => {
            searchQuery = e.target.value.trim().toLowerCase();
            filterProducts();
        });
    }
});
